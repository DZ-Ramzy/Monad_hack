Photos of the physical cards held in custody.

Each file here is hashed into the attestation committed onchain by
`pnpm seed:vault`, so the photo cannot be swapped after the fact without the
hash no longer matching.
